{% extends 'layout.twig' %}

{% block content %}
  <div class="row">
    <div class="col-3">
      <a href="{{ 'admin/combat_unit' | url}}" class="btn btn-info btn-lg btn-block">Unidades de Combate</a>
    </div>
    <div class="col-3">
      <a href="{{ 'admin/rank' | url}}" class="btn btn-info btn-lg btn-block">Rangos</a>
    </div>
    <div class="col-3">
      <a href="{{ 'admin/permission' | url}}" class="btn btn-info btn-lg btn-block">Permisos</a>
    </div>
    <div class="col-3">
      <a href="{{ 'admin/points_card' | url}}" class="btn btn-info btn-lg btn-block">Carta de Puntos</a>
    </div>
  </div>
{% endblock %}